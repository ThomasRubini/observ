apiVersion: v1
kind: Service
metadata:
  name: observ
  labels:
    app: observ
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: observ